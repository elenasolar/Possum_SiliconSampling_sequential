# self-check report (regenerated by `make check`)
*_check_report.txt

# maintainer/owner tooling — kept locally, never shipped to participating teams
maintainer/

# R / editor / OS cruft
.Rhistory
.RData
.Rproj.user/
.DS_Store

# for now, exclude all data and data subfolders
# modify to publish specific ones only if needed
data/